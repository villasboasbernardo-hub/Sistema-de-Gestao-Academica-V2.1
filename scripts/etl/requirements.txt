# Dependencias do ETL — Epico 2.
#
# POR QUE VERSAO FIXADA: e o mesmo principio do `pnpm-lock.yaml`. O CI, a maquina
# do operador e a maquina de quem for auditar a migracao em 2028 precisam ler os
# mesmos bytes. Numa migracao que roda UMA vez com dado historico, "funcionou na
# minha maquina" nao e anedota — e perda de dado.

openpyxl==3.1.5                    # etapa 1-B: leitura dos .xlsx da v1.0
psycopg[binary]==3.2.3             # PostgreSQL. Sem ORM (Principio III)
google-api-python-client==2.149.0  # etapa 1: Sheets API
google-auth==2.35.0                # credencial de conta de servico do ETL

# tzdata: OBRIGATORIA no Windows, onde o desenvolvimento acontece.
# O Windows nao traz base de fusos do sistema, e sem este pacote
# ZoneInfo("America/Sao_Paulo") levanta ZoneInfoNotFoundError — o ETL nem importa.
# Descoberto ao rodar o primeiro teste de fumaca, em 08/09/2026.
# No Linux do CI e redundante, mas fixa-la nos dois lugares e o que faz o
# comportamento ser o mesmo nas duas maquinas.
tzdata==2024.2
