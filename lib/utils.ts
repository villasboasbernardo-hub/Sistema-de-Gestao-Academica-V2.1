/**
 * Utilitário de composição de classe usado pelos componentes copiados do shadcn.
 *
 * ⚠️ ARQUIVO ESPERADO PELO `components.json` (alias `utils`). Ele reexporta o `cn` do pacote
 * homônimo, que é o do próprio shadcn — junção de classe com resolução de conflito do Tailwind.
 * Não é biblioteca de componentes: é um utilitário de cadeia de texto, e por isso não esbarra na
 * proibição do BRIEF §1.
 */
export { cn } from "cn";
